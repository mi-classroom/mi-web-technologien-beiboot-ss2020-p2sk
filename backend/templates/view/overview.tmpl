{{ template "header.tmpl" }}

<main>
    <div class="container section">
        {{ if .images }}
            {{template "image.tmpl" .images}}
        {{else}}
            <p>
                Keine Bilder verfügbar. Lade eins hoch!
            </p>
        {{end}}
    </div>
</main>

{{ template "footer.tmpl" }}